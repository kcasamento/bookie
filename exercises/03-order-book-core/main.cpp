#include "../../src/price_level.hpp"
#include "order_book.hpp"
#include <iostream>

// Exercise 03: see README.md in this directory for requirements.
// Replace this stub with your implementation.
int main() {
  // PriceLevel pl = PriceLevel(size_t(1050));
  // pl.addOrder(Order(1, size_t(1050), 10, Side::Buy));
  // pl.addOrder(Order(2, size_t(1050), 50, Side::Buy));
  //
  // bool ok = pl.addOrder(Order(1000, size_t(10), 10, Side::Buy));
  // std::cout << "bad order failed? " << !ok << "\n";
  //
  // Order o(3, size_t(950), 75, Side::Sell);
  // auto trades = pl.matchOrder(o);
  //
  // for (auto t : trades) {
  //   std::cout << "trade 1: " << t.size << "@ $" << t.trade_price << "\n";
  // }
  //
  // std::cout << "remaining to fill: " << o.quantity << "\n";
  //

  // OrderBook ob;
  // ob.addOrder(Order(1, 1050, 10, Side::Buy));
  // ob.addOrder(Order(2, 1050, 50, Side::Buy));
  //
  // auto [ok, trades] = ob.addOrder(Order(3, 1000, 20, Side::Sell));
  //
  // for (auto& t : trades) {
  //   std::cout << "trade #" << t.size << " $" << t.trade_price << "\n";
  // }
  //
  // std::cout << "best ask " << ob.bestBid().value() << "\n";
  //
  return 0;
}
